// Login used only inside the ephemeral database each Playwright config seeds
// for its own run — unrelated to any real account.
export const TEST_USERNAME = 'e2e-user'
export const TEST_PASSWORD = 'e2e-password'
