# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.*
!.env.example

# Common build/dependency dirs (harmless if unused by your stack)
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/

# Claude Code local state
.claude/settings.local.json

# NOT ignored, deliberately: .claude/.bootstrapped. It's tracked project
# state — git worktrees check out tracked files only, so ignoring it would
# make every parallel worktree look unconfigured and re-run bootstrap.

# --- appended by Vite react-ts scaffold ---
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

