project: job-applications
purpose: manages job applications
stack: React + Vite (TypeScript), frontend-only for now; server/ reserved for a Node API
bootstrapped: 2026-09-02

verified installed versions:
  node        24.20.0 (LTS "Krypton"; pinned in .nvmrc and package.json engines)
  npm         11.19.0
  typescript  6.0.3
  react       19.2.8
  react-dom   19.2.8
  vite        8.2.2
  vitest      4.1.11
  @testing-library/react 16.3.3
  jsdom       30.0.1
  @playwright/test 1.62.1
  oxlint      1.79.0

commands: build `npm run build` | test `npm test` | e2e `npm run test:e2e` | lint `npm run lint` | dev `npm run dev`
deferred: no formatter configured; production-gate.sh and protected-paths.sh left at defaults
